language: generic
sudo: required
services:
  -docker

before_install:
  - docker build -t EliranBD/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true EliranBD/docker-react npm run test
